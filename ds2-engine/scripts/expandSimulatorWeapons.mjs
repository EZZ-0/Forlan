/**
 * Sprint 6A — generate SIMULATOR_WEAPONS entries from weaponsFull.ts (manual review required).
 * Run after: npm run build
 * Status: scaffold only; full armory merge is data-heavy.
 */
console.log("expandSimulatorWeapons: use weaponsFull.ts + upgrade tables — implement in 6A sprint.");
