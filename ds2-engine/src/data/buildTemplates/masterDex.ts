import { createPlaceholderBuild } from "./placeholder";

export const masterDex = createPlaceholderBuild(
  "master-dex",
  "Master Dex Build",
  "Pure DEX. Rapiers, katanas, bows. High agility.",
  "swordsman"
);
