/** Shared style tokens for views — mirrors App.tsx legacy tokens */
import { colors } from "../theme";

export const bg = colors.bg;
export const bgCard = colors.bgCard;
export const gold = colors.gold;
export const ember = colors.ember;
export const dimText = colors.dimText;
export const lightText = colors.lightText;
export const warnRed = colors.warnRed;
export const successGreen = colors.successGreen;
